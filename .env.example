## Ejemplo de variables de entorno para desarrollo
## Copia este archivo a `.env` y reemplaza los valores por secretos reales.

# Secreto JWT para firmar tokens (no subir este archivo a git)
JWT_SECRET=changeme_replace_with_secure_value

# Opcional: URL base de la API si necesitas apuntar a otra ubicación
# API_BASE_URL=http://localhost:8000
